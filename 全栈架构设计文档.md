# ThinkWeave 全栈架构设计文档

> 更新日期：2026年4月11日
> 前后端分离架构 | 6 ThinkLets 完整支持

---

## 一、架构总览

```
ThinkWeave/
├── frontend/          # React 18 + TypeScript + Vite
├── backend/           # Python FastAPI + SQLAlchemy + PostgreSQL
├── 项目功能文档.md
├── 前端架构设计文档.md  (旧，已合并到此文档)
└── 全栈架构设计文档.md  (本文档)
```

### 通信方式

```
Frontend (React SPA)
    │
    ├── REST API  ──────►  Backend (FastAPI)  ──►  PostgreSQL
    │                          │
    ├── SSE Stream ◄────────── ┤  (AI流式响应)
    │                          │
    └── WebSocket ◄──────────► ┤  (Socket.IO实时协作)
                               │
                               ├──►  DeepSeek API
                               ├──►  文心一言 API
                               ├──►  通义千问 API
                               └──►  OpenAI API (GPT-4)
```

---

## 二、6种 ThinkLets 完整设计

### 2.1 ThinkLets 分类与流程

| ThinkLet | 中文名 | 类别 | 核心机制 | AI能力 |
|----------|--------|------|----------|--------|
| **FreeBrainstorm** | 自由头脑风暴 | 发散生成 | 自由产出想法，无约束 | 观点生成、精炼提取 |
| **LeafHopper** | 接龙发散 | 发散生成 | 多轮跳跃发散，基于前人观点拓展 | 观点生成、关联拓展 |
| **FastFocus** | 快速聚焦 | 聚焦澄清 | 逐条表决Keep/Merge/Discard | 相似度检测、精炼提取 |
| **BucketWalk** | 桶分类 | 归类整理 | 预设分类桶，拖放归类 | 语义聚类、自动归类 |
| **PopcornSort** | 米花拾掇 | 归类整理 | 逐条筛选+AI聚类去重 | 语义聚类、去重合并 |
| **StrawPoll** | 麦秆投票 | 评估投票 | 分配点数投票排序 | 结果汇总分析 |

### 2.2 公共能力（提取为共享模块）

所有 ThinkLets 共享以下 AI 能力，后端提取为公共服务：

| 公共能力 | 后端服务 | 使用场景 |
|---------|---------|---------|
| **观点精炼提取** | `IdeaExtractionService` | 从AI/用户长文本中提取独立观点 |
| **语义聚类** | `SemanticClusterService` | PopcornSort、BucketWalk 中分组 |
| **相似度检测** | `SimilarityService` | FastFocus 合并建议、PopcornSort 去重 |
| **AI观点生成** | `IdeaGenerationService` | FreeBrainstorm、LeafHopper 中AI发散 |
| **结果汇总** | `SummaryService` | StrawPoll 结果分析、全流程报告 |

### 2.3 各 ThinkLet 详细流程

#### FreeBrainstorm（自由头脑风暴）
```
开始 → 所有人+AI 自由输入观点 → 自动提取进灵感池 → 倒计时结束 → 输出观点池
```
- 前端：聊天式输入 + 右侧灵感池
- AI：并行多模型发散，自动提取关键观点进灵感池
- 数据：每条观点记录 author/round=1/timestamp

#### LeafHopper（接龙发散）
```
Round 1: 种子观点（来自上一环节或手动输入）
Round 2: 每人基于R1的观点向新方向跳跃
Round N: 重复直到轮次用完
```
- 前端：卡片式展示，每轮基于选中的前一轮观点发散
- AI：智能选择跳跃方向、补充盲区观点
- 数据：记录 round 编号 + parentIdeaId 跳跃链路

#### FastFocus（快速聚焦）
```
逐条展示观点 → 每人投票 Keep/Merge/Discard → AI检测相似项建议合并 → 输出精简列表
```
- 前端：逐条卡片展示，三按钮投票
- AI：标记高相似度对，建议合并文案
- 数据：每条投票记录 voteType + comment

#### BucketWalk（桶分类）
```
创建桶 → 逐条观点拖入桶 → AI辅助自动归类建议 → 输出分桶结果
```
- 前端：左侧观点列表 + 右侧桶区域，拖拽/点击归类
- AI：语义分析建议归类、标记异常项
- 数据：记录 ideaId→bucketId 映射

#### PopcornSort（米花拾掇）
```
全部观点卡片展示 → 点击选中保留 → AI语义聚类分组 → 去重合并 → 输出精选列表
```
- 前端：网格卡片，点击选中高亮
- AI：语义聚类虚线框、自动去重
- 数据：记录 isSelected 状态

#### StrawPoll（麦秆投票）
```
展示精选观点 → 每人分配N点 → 拖分/点选投票 → 汇总排名 → 输出优先级列表
```
- 前端：观点列表+投票条，实时排名更新
- AI：分析投票分布、生成排名报告
- 数据：记录 voterId + ideaId + points

---

## 三、后端架构 (backend/)

### 3.1 技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| **框架** | FastAPI | 异步、OpenAPI文档自动生成、类型安全 |
| **ORM** | SQLAlchemy 2.0 (async) | 异步ORM，声明式模型 |
| **数据库** | PostgreSQL 15 | 关系型数据库，支持JSONB |
| **迁移** | Alembic | 数据库版本管理 |
| **认证** | JWT (PyJWT) | 无状态认证 |
| **实时** | Socket.IO (python-socketio) | WebSocket实时通信 |
| **AI网关** | httpx (async) | 统一AI模型调用 |
| **缓存** | Redis | 会话缓存、在线状态、限流 |
| **校验** | Pydantic v2 | 请求/响应模型校验 |

### 3.2 后端目录结构

```
backend/
├── app/
│   ├── main.py                    # FastAPI应用入口
│   ├── config.py                  # 配置（环境变量）
│   ├── database.py                # 数据库连接
│   │
│   ├── models/                    # SQLAlchemy ORM模型 ★
│   │   ├── __init__.py
│   │   ├── user.py                # 用户表
│   │   ├── room.py                # 房间表
│   │   ├── room_member.py         # 房间成员关联表
│   │   ├── agent_config.py        # Agent配置表
│   │   ├── message.py             # 对话消息表
│   │   ├── workflow.py            # 流程步骤表
│   │   ├── idea.py                # 观点/灵感表（核心共享表）
│   │   ├── idea_vote.py           # FastFocus投票表
│   │   ├── bucket.py              # BucketWalk桶定义表
│   │   ├── bucket_assignment.py   # 桶归类映射表
│   │   ├── poll_ballot.py         # StrawPoll投票表
│   │   ├── knowledge_file.py      # 知识库文件表
│   │   └── subscription.py        # 订阅表
│   │
│   ├── schemas/                   # Pydantic请求/响应模型
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── room.py
│   │   ├── message.py
│   │   ├── workflow.py
│   │   ├── idea.py                # 共享的观点Schema
│   │   ├── thinklet.py            # 6种ThinkLet操作Schema
│   │   └── agent.py
│   │
│   ├── api/                       # 路由层
│   │   ├── __init__.py
│   │   ├── deps.py                # 依赖注入（认证、DB会话）
│   │   ├── auth.py                # 认证路由
│   │   ├── rooms.py               # 房间CRUD
│   │   ├── messages.py            # 消息路由
│   │   ├── workflows.py           # 流程管理
│   │   ├── ideas.py               # 观点CRUD（公共路由）
│   │   ├── thinklets.py           # ThinkLet环节操作路由
│   │   ├── agents.py              # AI调用路由
│   │   ├── files.py               # 文件上传
│   │   └── subscriptions.py       # 订阅付费
│   │
│   ├── services/                  # 业务逻辑层 ★
│   │   ├── __init__.py
│   │   ├── room_service.py
│   │   ├── message_service.py
│   │   ├── workflow_service.py
│   │   │
│   │   ├── shared/                # ★★★公共AI能力服务
│   │   │   ├── __init__.py
│   │   │   ├── idea_extraction.py     # 观点精炼提取（公共）
│   │   │   ├── idea_generation.py     # AI观点生成（公共）
│   │   │   ├── semantic_cluster.py    # 语义聚类（公共）
│   │   │   ├── similarity.py          # 相似度检测（公共）
│   │   │   └── summary.py            # 结果汇总（公共）
│   │   │
│   │   ├── thinklets/             # ★★★各ThinkLet业务逻辑
│   │   │   ├── __init__.py
│   │   │   ├── base.py               # ThinkLet基类（公共接口）
│   │   │   ├── free_brainstorm.py     # 自由头脑风暴
│   │   │   ├── leaf_hopper.py         # 接龙发散
│   │   │   ├── fast_focus.py          # 快速聚焦
│   │   │   ├── bucket_walk.py         # 桶分类
│   │   │   ├── popcorn_sort.py        # 米花拾掇
│   │   │   └── straw_poll.py          # 麦秆投票
│   │   │
│   │   └── ai/                    # AI模型调用网关
│   │       ├── __init__.py
│   │       ├── gateway.py             # 统一AI调用网关
│   │       ├── providers/
│   │       │   ├── __init__.py
│   │       │   ├── deepseek.py
│   │       │   ├── wenxin.py
│   │       │   ├── qianwen.py
│   │       │   └── openai_provider.py
│   │       └── prompts.py             # Prompt模板
│   │
│   └── realtime/                  # WebSocket实时层
│       ├── __init__.py
│       ├── socket_server.py       # Socket.IO服务端
│       ├── room_events.py         # 房间事件处理
│       └── thinklet_events.py     # ThinkLet环节实时事件
│
├── alembic/                       # 数据库迁移
│   ├── env.py
│   └── versions/
│
├── tests/
│   ├── test_api/
│   ├── test_services/
│   └── conftest.py
│
├── alembic.ini
├── requirements.txt
├── .env.example
└── Dockerfile
```

---

## 四、数据库设计 (PostgreSQL)

### 4.1 ER关系图

```
users ──1:N──► room_members ◄──N:1── rooms
  │                                     │
  │            agent_configs ◄──N:1─────┤
  │                                     │
  │            workflow_steps ◄──N:1────┤
  │                                     │
  └──1:N──► messages ◄──N:1────────────┘
  │                                     │
  └──1:N──► ideas ◄──N:1──────────────┘
  │             │
  │             ├──1:N──► idea_votes (FastFocus)
  │             ├──N:1──► bucket_assignments ──N:1──► buckets
  │             └──1:N──► poll_ballots (StrawPoll)
  │
  └──1:N──► knowledge_files
```

### 4.2 核心表结构

详见 `backend/app/models/` 各文件。

---

## 五、前端架构更新

### 5.1 新增的 ThinkLet 页面路由

| 路由 | ThinkLet | 页面 |
|------|----------|------|
| `/session/:id/brainstorm` | FreeBrainstorm | 自由发散 |
| `/session/:id/leafhopper` | LeafHopper | 接龙发散 |
| `/session/:id/fastfocus` | FastFocus | 快速聚焦 |
| `/session/:id/bucketwalk` | BucketWalk | 桶分类 |
| `/session/:id/popcornsort` | PopcornSort | 米花拾掇 |
| `/session/:id/strawpoll` | StrawPoll | 麦秆投票 |
| `/session/:id/report` | - | 汇总报告 |

### 5.2 公共前端函数（从各ThinkLet提取）

| 函数 | 文件 | 使用场景 |
|------|------|---------|
| `extractIdeasFromText()` | `shared/ideaExtraction.ts` | 所有环节的AI观点提取 |
| `useIdeaPool()` | `shared/useIdeaPool.ts` | 灵感池管理（增删改查+选中） |
| `useStepTimer()` | `shared/useStepTimer.ts` | 环节倒计时+紧急状态 |
| `useMemberSync()` | `shared/useMemberSync.ts` | 成员面板+在线状态同步 |
| `useAIResponse()` | `shared/useAIResponse.ts` | 多AI并行请求+流式响应 |
| `StepProgressBar` | `shared/StepProgressBar.tsx` | 流程进度条组件 |
| `IdeaCard` | `shared/IdeaCard.tsx` | 通用观点卡片组件 |
| `MemberPanel` | `shared/MemberPanel.tsx` | 通用成员面板组件 |

---

*完整的文件级实现详见各源代码中的TODO注释*
