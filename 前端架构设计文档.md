# ThinkWeave (AI Co-Lab) 前端架构设计文档

> 文档日期：2026年4月11日  
> 项目名：ThinkWeave — 多AI人机协作研讨平台  
> 当前状态：从静态HTML原型 → 工程化React应用

---

## 第一步：系统理解与抽象

### 1.1 核心功能模块（按业务划分）

| 模块 | 说明 | AI相关 |
|------|------|--------|
| **房间管理** | 创建/加入/邀请/状态显示 | ❌ |
| **对话交互** | 消息发送/AI响应/消息操作(采纳/打分/复制) | ✅ 多AI并行响应 |
| **多Agent管理** | AI模型选择/角色配置/参数调优/知识库 | ✅ 核心Agent调度 |
| **ThinkLets流程引擎** | 流程模板/环节设计/AI智能设计/进度控制 | ✅ AI流程生成 |
| **头脑风暴** | 灵感池/观点采集/手动添加/实时收集 | ✅ AI观点提取 |
| **观点收敛(米花拾掇)** | 筛选/语义聚类/去重合并/投票 | ✅ AI语义分析 |
| **订阅付费** | 套餐展示/周期切换/升级入口 | ❌ |
| **成员管理** | 人机成员列表/角色权限/在线状态 | ❌ |
| **历史记录** | 对话存储/加载/管理 | ❌ |
| **实时协作** | WebSocket通信/多人同步/状态广播 | ✅ Agent实时推送 |

### 1.2 分层架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                      页面层 (Pages)                          │
│  首页 | 创建房间 | 头脑风暴 | 米花拾掇 | 投票 | 报告 | 付费    │
├──────────────────────────────────────────────────────────────┤
│                    组件层 (Components)                        │
│  通用UI | 对话气泡 | 成员面板 | 灵感卡片 | 流程设计器 | 模态框  │
├──────────────────────────────────────────────────────────────┤
│                   Feature模块 (Features)                      │
│  chat/ | room/ | agent/ | thinklet/ | brainstorm/ | opinion/ │
├──────────────────────────────────────────────────────────────┤
│                 Agent调度层 (Agent Orchestrator)               │
│  模型注册 | 并行调度 | 响应聚合 | 角色管理 | 知识库注入          │
├──────────────────────────────────────────────────────────────┤
│                   状态管理 (Store/Zustand)                     │
│  roomStore | chatStore | agentStore | thinkletStore           │
├──────────────────────────────────────────────────────────────┤
│                   服务层 (Services/API)                        │
│  HTTP Client | WebSocket | AI API | Room API | User API       │
├──────────────────────────────────────────────────────────────┤
│                   工具层 (Utils/Hooks)                         │
│  计时器 | 文件处理 | 剪贴板 | 动画 | Toast | 类型定义           │
└──────────────────────────────────────────────────────────────┘
```

### 1.3 多AI/Agent相关模块标识

- `src/agents/` — **核心**：Agent注册、调度、响应聚合
- `src/features/chat/` — AI消息渲染、多模型并行响应
- `src/features/brainstorm/` — AI观点提取、灵感池自动采集
- `src/features/opinion/` — AI语义聚类、去重合并
- `src/features/thinklet/` — AI智能流程设计
- `src/store/agentStore.ts` — 全局Agent状态（模型列表/配置/在线状态）

---

## 第二步：技术架构设计

### 2.1 推荐技术栈

| 层次 | 技术选型 | 理由 |
|------|---------|------|
| **框架** | React 18 + TypeScript | 组件化、生态丰富、类型安全 |
| **构建工具** | Vite 5 | 极速HMR、原生ESM |
| **路由** | React Router v6 | 客户端SPA路由，适合协作平台 |
| **状态管理** | Zustand | 轻量、无boilerplate、适合多store拆分 |
| **服务端状态** | TanStack Query (React Query) | API缓存、自动重试、乐观更新 |
| **样式** | TailwindCSS 3 + CSS Modules | 延续现有设计系统，组件级隔离 |
| **图标** | Font Awesome 6 | 延续现有设计 |
| **实时通信** | Socket.IO Client | WebSocket封装、自动重连、房间机制 |
| **表单** | React Hook Form + Zod | 类型安全校验 |
| **HTTP** | Axios | 拦截器、错误处理 |
| **测试** | Vitest + Testing Library | 与Vite深度集成 |
| **代码规范** | ESLint + Prettier | 一致代码风格 |

### 2.2 架构模式

**Feature-Based 模块化架构** + **Agent Orchestrator 模式**

```
特征：
1. 每个业务功能自成一个 feature 目录，包含自己的组件、hooks、类型
2. Agent调度层独立为顶层模块，不嵌入任何feature
3. 状态管理按领域拆分 store（非单一Redux大仓库）
4. 服务层统一封装，feature通过hooks消费
```

### 2.3 特殊架构需求

#### 多Agent调度层 — 需要
```
AgentOrchestrator
├── AgentRegistry       — 注册可用AI模型（DeepSeek/文心/通义/GPT-4）
├── AgentDispatcher     — 并行分发请求到多个Agent
├── ResponseAggregator  — 聚合多Agent响应、去重、排序
├── AgentConfigManager  — 管理Agent角色模板/参数/知识库
└── AgentStreamHandler  — 处理SSE/WebSocket流式响应
```

#### WebSocket / 实时通信 — 需要
```
场景：
- 多人房间实时消息同步
- AI响应流式推送
- 成员在线状态广播
- 环节切换同步
- 投票实时更新
```

#### 插件化设计 — 需要（ThinkLets）
```
ThinkLets 流程环节采用插件式注册：
- 每种 ThinkLet 类型（FreeBrainstorm/PopcornSort/StrawPoll/SixHats/SWOT/Summary）
  注册为独立插件
- 新增分析方法时只需添加插件文件，无需修改核心流程引擎
```

#### 工作流引擎（ThinkLets）
```
实现方式：有限状态机 (FSM)
- 每个环节是一个状态节点
- 包含：进入条件 → 执行逻辑 → 退出条件 → 下一状态
- 支持模板加载、动态添加/删除环节
- 支持AI智能设计生成完整流程
```

---

## 第三步：项目目录结构

```
frontend/
├── public/                          # 静态资源（现有HTML原型保留参考）
│   ├── favicon.ico
│   └── assets/                      # 图片/字体等静态文件
│
├── src/
│   ├── app/                         # 【应用入口与全局配置】
│   │   ├── App.tsx                  # 根组件，路由出口
│   │   ├── main.tsx                 # Vite入口
│   │   ├── router.tsx               # 路由配置
│   │   └── providers.tsx            # 全局Provider组合（Query/Socket/Theme）
│   │
│   ├── pages/                       # 【页面层 — 路由级页面组件】
│   │   ├── HomePage/                # 首页（对话 + 成员管理）
│   │   │   ├── index.tsx
│   │   │   └── HomePage.module.css
│   │   ├── CreateRoomPage/          # 创建高级房间
│   │   │   ├── index.tsx
│   │   │   └── CreateRoomPage.module.css
│   │   ├── BrainstormPage/          # 自由头脑风暴环节
│   │   │   ├── index.tsx
│   │   │   └── BrainstormPage.module.css
│   │   ├── PopcornSortPage/         # 米花拾掇（观点筛选）
│   │   │   ├── index.tsx
│   │   │   └── PopcornSortPage.module.css
│   │   ├── StrawPollPage/           # 麦杆投票（待开发）
│   │   │   └── index.tsx
│   │   ├── ReportPage/              # 生成报告（待开发）
│   │   │   └── index.tsx
│   │   └── PricingPage/             # 付费页面
│   │       ├── index.tsx
│   │       └── PricingPage.module.css
│   │
│   ├── features/                    # 【Feature模块 — 按业务领域划分】★核心
│   │   ├── chat/                    # 对话交互功能
│   │   │   ├── components/          #   ChatWindow, MessageBubble, MessageInput
│   │   │   ├── hooks/               #   useChat, useMessageActions
│   │   │   ├── types.ts             #   Message, ChatState 类型
│   │   │   └── index.ts             #   模块导出
│   │   │
│   │   ├── room/                    # 房间管理功能
│   │   │   ├── components/          #   RoomHeader, InviteModal, JoinRoomModal
│   │   │   ├── hooks/               #   useRoom, useInvite
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── agent/                   # AI成员管理功能（面板/配置UI）
│   │   │   ├── components/          #   AgentPanel, AgentConfigModal, AgentCard
│   │   │   ├── hooks/               #   useAgentConfig
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── thinklet/                # ThinkLets流程功能
│   │   │   ├── components/          #   WorkflowDesigner, StepCard, TemplateSelector
│   │   │   ├── plugins/             #   各ThinkLet类型插件
│   │   │   ├── hooks/               #   useWorkflow, useThinkletEngine
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── brainstorm/              # 头脑风暴功能
│   │   │   ├── components/          #   IdeaPool, IdeaCard, BrainstormInput
│   │   │   ├── hooks/               #   useBrainstorm, useIdeaPool
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── opinion/                 # 观点筛选/聚类功能
│   │   │   ├── components/          #   PopcornCard, ClusterGroup, FilterBar
│   │   │   ├── hooks/               #   useOpinionFilter, useSemanticCluster
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── member/                  # 成员管理功能
│   │   │   ├── components/          #   MemberPanel, MemberCard, DrawerToggle
│   │   │   ├── hooks/               #   useMembers
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── subscription/            # 订阅付费功能
│   │   │   ├── components/          #   PricingCard, BillingToggle, FeatureCompare
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   └── history/                 # 历史记录功能
│   │       ├── components/          #   HistoryList, HistoryItem
│   │       ├── hooks/               #   useHistory
│   │       ├── types.ts
│   │       └── index.ts
│   │
│   ├── agents/                      # 【Agent调度层 — 多AI协作核心】★★★
│   │   ├── AgentRegistry.ts         # Agent模型注册表
│   │   ├── AgentDispatcher.ts       # 并行请求分发器
│   │   ├── ResponseAggregator.ts    # 多Agent响应聚合
│   │   ├── AgentConfigManager.ts    # Agent配置管理（模板/参数/知识库）
│   │   ├── AgentStreamHandler.ts    # 流式响应处理（SSE）
│   │   ├── plugins/                 # Agent能力插件（语义聚类/观点提取等）
│   │   │   ├── SemanticClusterPlugin.ts
│   │   │   ├── IdeaExtractionPlugin.ts
│   │   │   └── WorkflowDesignPlugin.ts
│   │   ├── types.ts                 # Agent相关类型定义
│   │   └── index.ts
│   │
│   ├── services/                    # 【服务层 — API通信】
│   │   ├── api/
│   │   │   ├── client.ts            # Axios实例 + 拦截器
│   │   │   ├── roomApi.ts           # 房间CRUD API
│   │   │   ├── chatApi.ts           # 对话消息API
│   │   │   ├── agentApi.ts          # AI模型调用API
│   │   │   ├── userApi.ts           # 用户/认证API
│   │   │   ├── subscriptionApi.ts   # 订阅付费API
│   │   │   └── fileApi.ts           # 文件/知识库上传API
│   │   ├── websocket/
│   │   │   ├── socketClient.ts      # Socket.IO客户端封装
│   │   │   ├── roomSocket.ts        # 房间实时事件
│   │   │   └── types.ts             # Socket事件类型
│   │   └── index.ts
│   │
│   ├── store/                       # 【状态管理 — Zustand stores】
│   │   ├── roomStore.ts             # 房间状态（当前房间/配置/成员）
│   │   ├── chatStore.ts             # 对话状态（消息列表/发送状态）
│   │   ├── agentStore.ts            # Agent状态（模型列表/配置/活跃Agent）
│   │   ├── thinkletStore.ts         # 流程状态（当前环节/步骤/进度）
│   │   ├── brainstormStore.ts       # 头脑风暴状态（灵感池/观点列表）
│   │   ├── uiStore.ts              # UI状态（面板开关/模态框/Toast）
│   │   └── index.ts
│   │
│   ├── components/                  # 【通用组件 — 跨Feature复用】
│   │   ├── ui/                      # 基础UI组件
│   │   │   ├── Button.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Toast.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Select.tsx
│   │   │   ├── Toggle.tsx
│   │   │   ├── Badge.tsx
│   │   │   ├── Avatar.tsx
│   │   │   ├── Slider.tsx
│   │   │   └── FileUpload.tsx
│   │   ├── layout/                  # 布局组件
│   │   │   ├── AppLayout.tsx        # 全局布局（导航+侧边栏+主区域）
│   │   │   ├── Sidebar.tsx          # 左侧导航/历史栏
│   │   │   └── Header.tsx           # 顶部导航栏
│   │   ├── feedback/                # 反馈组件
│   │   │   ├── ToastContainer.tsx
│   │   │   ├── LoadingSpinner.tsx
│   │   │   └── ProgressBar.tsx
│   │   └── timer/                   # 计时组件
│   │       ├── Countdown.tsx        # 倒计时组件
│   │       └── RoomTimer.tsx        # 房间运行时长
│   │
│   ├── hooks/                       # 【全局Hooks — 跨Feature复用】
│   │   ├── useCountdown.ts          # 倒计时逻辑
│   │   ├── useClipboard.ts          # 剪贴板操作
│   │   ├── useWebSocket.ts          # WebSocket连接管理
│   │   ├── useToast.ts              # Toast通知
│   │   ├── useAuth.ts               # 认证状态
│   │   └── useMediaQuery.ts         # 响应式断点
│   │
│   ├── utils/                       # 【工具函数】
│   │   ├── formatTime.ts            # 时间格式化
│   │   ├── formatFileSize.ts        # 文件大小格式化
│   │   ├── validators.ts            # 通用校验
│   │   └── constants.ts             # 全局常量（AI模型定义/ThinkLet类型等）
│   │
│   ├── types/                       # 【全局类型定义】
│   │   ├── models.ts                # 业务实体类型（Room/Message/Agent/ThinkLet）
│   │   ├── api.ts                   # API请求/响应类型
│   │   └── enums.ts                 # 枚举类型（ThinkLetType/AgentRole/MemberRole）
│   │
│   └── styles/                      # 【全局样式】
│       ├── globals.css              # Tailwind指令 + 全局样式
│       ├── animations.css           # 动画定义（现有动效迁移）
│       └── scrollbar.css            # 滚动条自定义
│
├── index.html                       # Vite HTML入口
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
├── .eslintrc.cjs
├── .prettierrc
├── .env.example                     # 环境变量模板
└── .gitignore
```

---

## 第四步：核心模块拆解

### 4.1 页面职责

| 页面 | 路由 | 核心职责 |
|------|------|----------|
| **HomePage** | `/` | 主对话界面、AI选择、成员管理、历史记录、已采纳意见 |
| **CreateRoomPage** | `/room/create` | 填写房间配置、设计ThinkLets流程、AI智能设计 |
| **BrainstormPage** | `/session/:id/brainstorm` | 执行头脑风暴环节、灵感池管理、AI多模型响应 |
| **PopcornSortPage** | `/session/:id/popcorn-sort` | 观点筛选卡片、AI语义聚类、去重合并 |
| **StrawPollPage** | `/session/:id/straw-poll` | 投票排序（待开发） |
| **ReportPage** | `/session/:id/report` | AI生成研讨报告（待开发） |
| **PricingPage** | `/pricing` | 套餐展示、付费周期切换 |

### 4.2 关键组件作用

| 组件 | 所属Feature | 作用 |
|------|------------|------|
| `ChatWindow` | chat | 聊天窗口容器，管理消息列表滚动和AI响应流 |
| `MessageBubble` | chat | 单条消息渲染（区分user/ai），悬停操作栏(采纳/打分/复制) |
| `MessageInput` | chat | 输入框组件，支持Enter/Shift+Enter、自动高度 |
| `AgentPanel` | agent | 右侧成员面板中AI成员区域，显示AI列表和状态 |
| `AgentConfigModal` | agent | AI自定义配置模态框（模型/模板/参数/知识库） |
| `WorkflowDesigner` | thinklet | 流程设计器，拖拽环节、模板加载、AI设计 |
| `StepCard` | thinklet | 单个流程环节卡片（标题/时长/Prompt/删除） |
| `IdeaPool` | brainstorm | 灵感池面板，展示自动采集的观点卡片 |
| `IdeaCard` | brainstorm | 单张灵感卡片（内容/来源/颜色/编辑/删除） |
| `PopcornCard` | opinion | 筛选用观点卡片（点击选中/取消，动画反馈） |
| `ClusterGroup` | opinion | AI聚类分组容器（虚线框+标签） |
| `MemberPanel` | member | 右侧成员面板（AI队友+人类团队，可折叠） |
| `InviteModal` | room | 邀请成员模态框（房间号/链接/密码/社交分享） |
| `PricingCard` | subscription | 套餐卡片（基础/专业/团队） |
| `Countdown` | components/timer | 通用倒计时组件，支持紧急状态闪烁 |
| `Modal` | components/ui | 通用模态框（淡入+滑上动画） |
| `Toast` | components/feedback | Toast提示（绿色成功、自动2s消失） |

### 4.3 Agent/多AI模块组织

```
请求流程：
用户发消息 → AgentDispatcher → 并行发送到选中的N个Agent
                                    ├─ DeepSeek V3 API
                                    ├─ 文心一言 API
                                    ├─ 通义千问 API
                                    └─ GPT-4 API
                              ← ResponseAggregator ← 流式聚合响应
                              → chatStore 更新 → UI渲染多条AI回复

Agent配置流程：
AgentConfigModal(UI) → agentStore → AgentConfigManager
    ├─ 模型选择
    ├─ 角色模板(system prompt)
    ├─ Temperature / MaxTokens
    └─ 知识库文件 → fileApi上传 → RAG注入

Agent注册机制：
AgentRegistry 维护一个 Map<AgentId, AgentConfig>
├─ 内置4种模型
├─ 支持运行时动态注册新Agent
└─ 每个Agent有：id, name, model, icon, color, systemPrompt, params
```

### 4.4 数据流总览

```
                    ┌─────────────┐
                    │  WebSocket  │ ← 实时事件（成员变化/环节切换/投票更新）
                    └──────┬──────┘
                           │
┌──────────┐     ┌─────────▼──────────┐     ┌──────────────┐
│  Pages   │ ──► │  Zustand Stores    │ ◄── │  API Service │
│ (UI层)   │ ◄── │  (状态中心)         │ ──► │  (HTTP/WS)   │
└──────────┘     └─────────┬──────────┘     └──────────────┘
                           │
                    ┌──────▼──────┐
                    │   Agent     │
                    │ Orchestrator│ ← 多模型并行调度
                    └─────────────┘

单向数据流：
1. 用户操作 → Action → Store更新 → UI重渲染
2. API响应 → Store更新 → UI重渲染  
3. WebSocket事件 → Store更新 → UI重渲染
4. Agent响应(流式) → chatStore追加 → MessageBubble渐进渲染
```

---

## 第五步：文件级TODO（详见各源文件）

所有关键源文件均已创建在 `frontend/src/` 下，内含具体可执行的TODO注释。覆盖：
- 7 个页面文件
- 15+ 核心组件
- 5 个 Agent 调度模块
- 7 个 Zustand Store
- 6 个 API 服务
- 2 个 WebSocket 模块
- 6+ 全局 Hooks
- 全局类型定义与常量

---

## 第六步：工程最佳实践建议

### 6.1 代码规范

```
- TypeScript strict模式，所有文件100%类型覆盖
- ESLint + Prettier 自动格式化
- 组件命名：PascalCase（MessageBubble.tsx）
- Hooks命名：camelCase + use前缀（useChat.ts）
- 类型文件：每个feature自带types.ts，全局类型在src/types/
- 导入顺序：React → 第三方 → @/features → @/components → @/utils → 相对路径
- 禁止 any 类型（eslint: @typescript-eslint/no-explicit-any）
```

### 6.2 可扩展性设计

```
1. Agent插件机制：新增AI模型只需在 AgentRegistry 注册，无需改动调度逻辑
2. ThinkLet插件：新增分析方法只需在 thinklet/plugins/ 添加插件文件
3. Feature模块独立：每个feature可独立开发/测试/懒加载
4. API层抽象：所有后端交互通过service层，后端更换不影响业务逻辑
5. Store职责单一：每个store只管理一个领域，避免"上帝store"
```

### 6.3 如何支持未来更多AI Agent

```
当前设计的扩展点：
├── AgentRegistry.register({ id, name, model, endpoint, icon, ... })
│   → 运行时热注册新Agent，无需重启
├── AgentDispatcher 基于 AgentRegistry 动态获取活跃Agent列表
│   → 新Agent自动参与调度
├── agentStore.agents 数组动态扩展
│   → UI自动渲染新Agent卡片
├── Agent能力插件(plugins/) 可独立添加
│   → 语义聚类/摘要/翻译…各能力可按Agent挂载
└── 知识库关联到Agent维度
    → 每个Agent可有独立知识库，RAG隔离
```

### 6.4 如何避免架构混乱

```
原则：
1. 「Feature-first」: 新功能先确定属于哪个feature，在该目录下开发
2. 「单向依赖」: pages → features → agents/services → utils（不反向引用）
3. 「组件层级」: 通用UI(components/) < Feature组件(features/*/components/) < 页面(pages/)
4. 「状态边界」: feature内部状态用hooks管理，跨feature状态用zustand store
5. 「API层隔离」: 组件永远不直接调API，通过hooks或store中转
6. 「类型先行」: 先定义types.ts再写实现，类型即文档
7. 「定期Review」: feature目录超过10个文件时考虑子模块拆分
```

---

*架构文档结束 — 下面的TODO文件按此架构实施*
